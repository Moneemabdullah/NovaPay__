# Ledger service environment (copy to .env for local development)
NODE_ENV=development
PORT=3003

# Postgres (own logical database per service — no shared DB)
DATABASE_URL=postgres://novapay:novapay@localhost:5432/ledger_db
